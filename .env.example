# OpenAI API Key
# Get your OpenAI API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Conversation Memory / Checkpointing (Optional)
# Enable/disable conversation memory via checkpointing (default: false)
ENABLE_CHECKPOINTING=false
# Path to SQLite checkpoint database (default: checkpoints.db)
CHECKPOINT_DB_PATH=checkpoints.db

# Phoenix Observability (Optional)
# Enable/disable Phoenix instrumentation (default: true)
PHOENIX_ENABLED=true
# Phoenix OTLP endpoint (default: http://localhost:4317)
PHOENIX_ENDPOINT=http://localhost:4317
